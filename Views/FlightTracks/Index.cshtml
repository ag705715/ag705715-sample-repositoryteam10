@model IEnumerable<MIS4200AndrewGolik.Models.FlightTrack>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.flight.departingAirport)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ticketID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.flight.departingAirport)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ticketID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.flightTrackID }) |
            @Html.ActionLink("Details", "Details", new { id=item.flightTrackID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.flightTrackID })
        </td>
    </tr>
}

</table>
